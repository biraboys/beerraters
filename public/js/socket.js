const socket=io.connect('/',{transports:['websocket']});socket.on('news',async function(a){const b=document.getElementById('user-session-id').href.split('/')[4],c=await fetch(`/users/${b}/following`,{method:'get',credentials:'same-origin'}),d=await c.json(),e=d.following.map(f=>{return f._id});-1<e.indexOf(a.user_id)&&($('#feed-link').addClass('pulse'),$('#feed-link').click(()=>{$('#feed-link').removeClass('pulse')}),$('#mobile-feed-link').addClass('pulse'),$('#mobile-feed-link').click(()=>{$('#mobile-feed-link').removeClass('pulse')}))});
