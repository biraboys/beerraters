<%- include header.ejs %>

<section class="main-section">
  <div class="container">
      <div class="row">
          <div class="col s12">
              <% if (success) { %>
                <h4>Success!</h4>
                <p><%= message %> <a href="/login">here</a></p>
              <% } else { %>
                <h4>Something went wrong here...</h4>
                <p><%= message %></p>
              <% } %>
          </div>
      </div>
  </div>
</section>

<%- include footer.ejs %>

</body>
</html>