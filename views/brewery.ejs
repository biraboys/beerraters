<%- include header.ejs %>

<div class="section hero">
 <div class="container">
   <div class="row">
         <% if (brewery === undefined) { %>
          <div class="one-half column">
            <h1 class="hero-heading">Sorry could not found brewery</h1>
          </div>
            <% } else { %>
          <div class="one-half column">
             <h2><%= brewery.name %></h2>
             <h4><%= brewery.state_id %></h4>
             <h4><%= brewery.country_id %></h4>
             <!--<img class="u-max-full-width" src="/images/flags/<%= country.flag_32 %>">-->
     </div>
     <div class="one-half column">
       <img class="u-max-full-width" src="/images/brewery.jpg">
     </div>
      <% } %>  
   </div>
 </div>
</div>

<%- include footer.ejs %>